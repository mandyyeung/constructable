<%= form_form @request do |f| %>
  <div class="form-group">
    <%= f.text_field :subject, placeholder: "Subject", class: "form-control" %>
    <%= f.textarea :body, placeholder: "Request", class:"form-control" %>
  </div>

  <div class="form-group">
    <%= f.submit %>
  </div>

<% end %>
